import { Component, OnInit, OnChanges, ChangeDetectionStrategy, ChangeDetectorRef, Input, Output } from '@angular/core';
import { FormControl } from '@angular/forms';

@Component({
	moduleId: module.id,
    selector: 'popular-lines',
    templateUrl: "./popular-lines.template.html"
})
export class popularLinesComponent implements OnInit {


	
	ngOnInit() {
		
	}

}